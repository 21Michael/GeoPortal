import React from 'react';
import PropTypes from 'prop-types';
import classes from './map.module.scss';

const BasinMap = (props) => {}

BasinMap.propTypes = {
    //products: PropTypes.array
}

export default BasinMap;